[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[target "myduit_test"]
	uri = db:pg:myduit_test
[engine "pg"]
	target = myduit_test
[deploy]
	verify = true
[rebase]
	verify = true
